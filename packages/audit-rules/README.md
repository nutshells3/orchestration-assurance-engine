# formal-claim-audit-rules

Deterministic assurance-profile computation rules extracted from prompt-only
policy logic.

Current scope:

- deterministic gate and allowed-downstream decisions
- vector score aggregation from typed verifier, audit, evidence, and coverage input
- coverage derivation from the canonical claim graph
- hard promotion rule validation independent of any LLM
