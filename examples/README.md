# Examples

Canonical golden and demo examples live here.

- `toy-claim/`
- `theorem-audit/`
- `paper-claim/`
- `legal-claim/`

Scenario replay fixtures live under the domain directories as:

- `source.md`: seed input for local document import
- `scenario.json`: deterministic fixture contract used by CLI/MCP/desktop replay
- `expected-summary.json`: normalized acceptance contract for the replayed project
