<!-- backlog-id: M5-02 -->
# [M5-02] Port selected `claim_tracer_mcp` ingest and query flows into canonical MCP endpoints

Generated from `docs/FORMAL_CLAIM_MONOREPO_BACKLOG.csv`. Treat the backlog CSV and companion backlog doc as the current planning source for this task.

## Current Baseline
- Status: `done`
- Wave: `workflow-mcp-cli`
- Execution mode: `implement`
- Prototype basis: Useful donor behaviors already existed in `claim_tracer_mcp`, but they needed to be absorbed into canonical dotted MCP endpoints without reviving legacy store/model ownership.

## Implementation Stance
This backlog item is recorded as completed and should only be reopened deliberately. Implement the target capability on top of the frozen canonical contracts and migrated services. Expose and compose stable internal workflows only after the contracts and runner surfaces have settled. Current prototype basis: Useful donor behaviors already existed in `claim_tracer_mcp`, but they needed to be absorbed into canonical dotted MCP endpoints without reviving legacy store/model ownership.

## Summary
Completed: canonical MCP endpoints now cover ingest, trace-forward, trace-backward, gap detection, assessment, graph export, and project/graph reads, while donor compatibility helpers remain outside the canonical tool registry.

## Acceptance Criteria
Tool names and JSON responses are documented; canonical MCP exposes `document.ingest`, `claim.trace_forward`, `claim.trace_backward`, `graph.detect_gaps`, `claim.assess`, and `graph.export`, and legacy user stories map onto the new server without reintroducing legacy models.

## Backlog Metadata
- Milestone: `M5 MCP`
- Package: `services/mcp-server`
- Path: `services/mcp-server`
- Type: `feat`
- Owner lane: `Research`
- Suggested executor: `Codex`
- Estimate: `M`
- Depends on: `M5-01`, `M4-06`
- Labels: `mcp`, `claim-tracer`, `ingestion`
