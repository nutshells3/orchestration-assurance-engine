<!-- backlog-id: M5-01 -->
# [M5-01] Define narrow typed MCP tool/resource facade over FormalClaimEngineAPI

Generated from `docs/FORMAL_CLAIM_MONOREPO_BACKLOG.csv`. Treat the backlog CSV and companion backlog doc as the current planning source for this task.

## Current Baseline
- Status: `done`
- Wave: `workflow-mcp-cli`
- Execution mode: `implement`
- Prototype basis: `services/mcp-server` already existed from the donor MCP surface, but it had to be re-scoped into an engine-owned thin facade rather than a second workflow owner.

## Implementation Stance
This backlog item is recorded as completed and should only be reopened deliberately. Implement the target capability on top of the frozen canonical contracts and migrated services. Expose and compose stable internal workflows only after the contracts and runner surfaces have settled. Current prototype basis: `services/mcp-server` already existed from the donor MCP surface, but it had to be re-scoped into an engine-owned thin facade rather than a second workflow owner.

## Summary
Completed: the canonical MCP server now exposes only workflow-boundary tools plus read-only resources over `FormalClaimEngineAPI`, uses job handles for long-running mutations, and keeps canonical artifact ownership inside engine.

## Acceptance Criteria
MCP server provides typed workflow tools for project CRUD, document ingest, claim structuring, dual formalization, audit, profile recomputation, promotion, and bundle export; resources are read-only and long-running operations return `job_id` handles.

## Backlog Metadata
- Milestone: `M5 MCP`
- Package: `services/mcp-server`
- Path: `services/mcp-server`
- Type: `feat`
- Owner lane: `Platform`
- Suggested executor: `Codex`
- Estimate: `M`
- Depends on: `M4-07`, `M3-07`
- Labels: `mcp`, `api`, `platform`
