<!-- backlog-id: M5-03 -->
# [M5-03] Expose proof/audit MCP workflow tools for engine-backed audit and profile recomputation

Generated from `docs/FORMAL_CLAIM_MONOREPO_BACKLOG.csv`. Treat the backlog CSV and companion backlog doc as the current planning source for this task.

## Current Baseline
- Status: `done`
- Wave: `workflow-mcp-cli`
- Execution mode: `implement`
- Prototype basis: Runner-backed audit and profile recomputation already existed behind engine workflows; MCP only needed to expose them as thin agent-facing control-plane calls.

## Implementation Stance
This backlog item is recorded as completed and should only be reopened deliberately. Implement the target capability on top of the frozen canonical contracts and migrated services. Expose and compose stable internal workflows only after the contracts and runner surfaces have settled. Current prototype basis: Runner-backed audit and profile recomputation already existed behind engine workflows; MCP only needed to expose them as thin agent-facing control-plane calls.

## Summary
Completed: MCP now exposes the proof workflow agents actually need through `formalize.dual`, `audit.run`, `profile.recompute`, `promotion.transition`, and `bundle.export`, with job results and artifact refs matching the engine API boundary.

## Acceptance Criteria
Canonical MCP covers formalization, runner-backed audit, assurance-profile recomputation, promotion transitions, and bundle export through the engine API; parity smoke confirms MCP results match direct engine calls for the end-to-end workflow.

## Backlog Metadata
- Milestone: `M5 MCP`
- Package: `services/mcp-server`
- Path: `services/mcp-server`
- Type: `feat`
- Owner lane: `Proof`
- Suggested executor: `Codex`
- Estimate: `S`
- Depends on: `M5-01`, `M4-04`
- Labels: `mcp`, `isabelle`, `audit`
